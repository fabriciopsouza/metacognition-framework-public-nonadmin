# QA evidence — v1.63.0 (ADR-086, recast B: risk_score)

**Junção:** PC · **Recomendação:** aprovar_com_ressalvas · **Autor:** Opus 4.8 → **crítico:** Sonnet (worktree isolado, ADR-078) · **Data:** 2026-06-17

## Escopo
Gating determinístico por risco (recast (B) do TEA/BMAD sob P15): `tools/risk_score.py` (`risco = prob × impacto` → gate + tier P0–P3), canário exaustivo, capability `risk-gate`, wire em `high-stakes-gate`. FORMA agnóstica no núcleo; categorias = input/blueprint (P12).

## QA (1 rodada, worktree)
PASS aprovar_com_ressalvas. **Canário provado não-teatro:** o crítico mutou a matriz (gate 6→ADVISORY; tier P0 `>=6`→`>=7`) em cópia temp → canário FAIL exit 1. Fail-closed, P12 e agregação confirmados. 3 ressalvas BAIXO corrigidas:
- type-guard estrito (`type is int`) — rejeita float (`1.0 ∈ (1,2,3)` é True) e bool (`True==1`).
- comentários de faixa esclarecidos (scores possíveis = {1,2,3,4,6,9}; 5/7/8 impossíveis).
- canário ganhou assert de `aggregate_gate([]) == NONE` + mix CONCERNS.

## Correção registrada
A "dependência do linter forma-vs-conteúdo" que o ADR-085 declarou como pré-requisito de (B) era **FALSA** (afirmada sem ler o linter; denylist-based já permite a forma). ADR-085 emendado; ADR-086-linter **não construído** (trave inútil evitada).

## Ressalva residual
A estimativa de prob/impacto por item é julgamento humano na ENTRADA — a porta do P15: o tool mecaniza cálculo→gate, não a estimativa (declarado).

## RRC
ADR-086 Aceito no CHANGELOG ✓ · capability `risk-gate` nova (test_capabilities PASS) ✓ · README+vitrine+web-bundles 1.63.0 ✓ · canário = guard ✓ · suite 55 PASS / 1 SKIP / 0 FAIL ✓.
