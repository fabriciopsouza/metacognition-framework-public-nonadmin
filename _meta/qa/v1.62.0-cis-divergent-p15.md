# QA evidence — v1.62.0 (ADR-085, bloco A + codificação de núcleo)

**Junção:** PC (process-critic) · **Recomendação:** aprovar_com_ressalvas · **Autor:** Opus 4.8 → **crítico:** Sonnet (worktree isolado, ADR-078) · **Data:** 2026-06-17

## Escopo
- (A) Fase divergente de elicitação: +7 métodos (#70–76, recast do CIS) + eixo de fase + fallback parada-e-orientação + canário fail-closed `test_elicitation_catalog.py`.
- Codificação de núcleo: §6 **P15** (determinismo-primeiro + porta + HITL por modo) + emendas **P10** (adoção externa) e **P12** (forma-vs-conteúdo).
- Re-avaliação BMAD org-wide 13/13 com fonte (`gh api`).

## QA — 3 rodadas (heterogêneo)
- **R1** (sem isolamento): INVALIDADA por artefato — o crítico rodou `git checkout` e apagou trabalho não-commitado, gerando FAIL falso. Lição: commitar antes / worktree isolado. Achados válidos separáveis aproveitados.
- **R2** (worktree): PASS aprovar_com_ressalvas — 2 buracos anti-JARVIS (corrigidos+provados), fidelidade CIS `[INFERIDO]`, distinção #76↔#9. Anti-rename ok.
- **R3** (worktree, codificação de núcleo): PASS aprovar_com_ressalvas — duplicação SSoT P15↔A.1 (corrigido: P15 enxugado→ponteiro), ADR Proposto governando §6 (corrigido: Aceito+CHANGELOG), auto-aplicação (corrigido: declaração canônica = capabilities.json).

## Ressalvas residuais
- Teste forma-vs-conteúdo (P12) ainda prosa no `check_core_agnostic.py` — mecanização é o ADR pré-requisito declarado de (B).
- P15 é hook-mediado: sob EDR depende de disciplina (esta sessão expôs — qa-evidence foi pulado até o dono cobrar na auditoria).

## RRC
ADR-085 Aceito no CHANGELOG ✓ · capability estendida (test_capabilities PASS) ✓ · README+vitrine+web-bundles 1.62.0 ✓ · canário novo = regressão-guard ✓ · suite 54 PASS / 1 SKIP / 0 FAIL ✓.

Ledger de junção: `_meta/qa/junctions/adr-085-a-cis-divergente.jsonl`.
