# Framework — não versionar
.agent/brain/
.env
.env.*
*.key
*.pem
**/credentials*.json
outputs/
output/

# v1.6.1 — flag de escape do auto-start-session (pessoal por cópia de trabalho)
.claude/session.lock
/WORKING-CONTEXT.md

# Claude Code scheduler internal
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json

# ADR-060/061: markers de liveness dos hooks (por sessao/maquina — nunca versionar)
.claude/.hooklive/
.claude/.repo-sync-marker
.claude/boot-proof.json
.claude/closing-proof.json

# Carimbo da ultima execucao da suite de canarios (ADR-115, 34a rodada). MEDICAO LOCAL,
# mesma classe de boot-proof/closing-proof: quem MEDE grava aqui, quem PUBLICA (projeto_docs)
# le. Nao versionar — a suite roda varias vezes por sessao, e arvore suja congela o hook de
# sync EM SILENCIO. O numero que precisa viajar ja' viaja dentro do STATUS-REPORT.md, com a
# data da medicao ao lado; sem o carimbo o quadro diz "nao medido", que e' a verdade.
_meta/canarios-ultimo.json

# ADR-084: marker de defasagem vs branch de integracao + ack (por branch/maquina — nunca versionar)
.claude/.stale-vs-main
.claude/.stale-vs-main.ack

# ADR-064: staging local de relatorios anonimizados (gerado, nao versionar no repo-fonte)
telemetry/learnings-public/

# ADR-065: estado da oferta de relatorio por solucao (por maquina; nao versionar)
.claude/.report-offers/

# ADR-068: catalogo de insights gerado offline (owner-only; nao distribuir)
docs/_private/_catalog/

# Permissoes pessoais (nao distribuir) — ADR-005/047
.claude/settings.local.json

# Config cross-IA com paths absolutos por maquina (nunca distribuir — ADR-069/070)
.claude/cross-ai-repos.json
